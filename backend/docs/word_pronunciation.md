**中文** | [English](word_pronunciation_en.md)

# 单词发音播放方案

> 📋 **功能说明**: 在发音反馈页面，用户点击任意单词时，播放该单词的正确发音。

## 🎯 设计目标

- ✅ 低延迟：用户点击后快速响应
- ✅ 多语言：支持英语、中文、日语、韩语等多种语言
- ✅ 高质量：发音准确，与评估标准一致
- ✅ 成本可控：通过缓存减少 API 调用

---

## 📐 混合方案架构

```
用户点击单词
    ↓
[1. 检查本地缓存] ───(有)──→ 直接播放 ✅
    ↓ (无)
[2. 检查语言是否本地 TTS 支持]
    ↓
  ┌─(支持)────→ [3a] 本地 TTS 播放 ✅（零成本）
  │
  └─(不支持)──→ [3b] 请求云端 TTS → 播放 → 缓存
```

---

## 🔀 云端 TTS 服务选型

> ✅ **最终决策**：选择 **Gemini 2.5 Flash Preview TTS** (通过 Vertex AI)
>
> 理由：已有 $25,000 GCP 额度，质量优秀，低延迟，支持流式输出。

### ✅ 方案: Gemini 2.5 Flash Preview TTS（已选择）

| 维度         | 说明                                                          |
| ------------ | ------------------------------------------------------------- |
| **状态**     | ✅ 已集成，`/tts/gcp/generate` (流式) 和 `/tts/word` (非流式) |
| **模型**     | `gemini-2.5-flash-preview-tts`                                |
| **多语言**   | ✅ 支持多语言，内置语音可自动适配                             |
| **延迟**     | 🚀 流式输出，首字节延迟低                                     |
| **输出格式** | WAV (PCM 24kHz 16-bit mono)                                   |
| **优势**     | $25,000 GCP 额度可用，质量高，低延迟                          |
| **音色**     | Kore(默认), Aoede, Charon, Fenrir, Puck, Orbit                |

#### 价格 (Vertex AI)

| 项目   | Free Tier      | Paid Tier (per 1M tokens) |
| ------ | -------------- | ------------------------- |
| Input  | Free of charge | $0.50 (text)              |
| Output | Free of charge | $10.00 (audio)            |

> 💡 **注意**：Preview 模型有 Free Tier 额度，且我们有 $25,000 GCP 信用额度。

<details>
<summary>方案 B: Azure Speech TTS（未使用）</summary>

| 维度       | 说明                        |
| ---------- | --------------------------- |
| **状态**   | 未集成                      |
| **多语言** | ✅ 140+ 语言，400+ 神经语音 |
| **延迟**   | ~100-200ms                  |
| **劣势**   | 需额外开发，无现有额度      |

</details>

---

## 💰 成本估算

### 基础假设

- 单词平均长度：7 字符
- 用户每天练习：100 个单词
- 月活跃用户：1000 DAU

### ✅ Gemini 2.5 Flash Preview TTS 成本（当前使用）

| 项目   | Free Tier      | Paid Tier          |
| ------ | -------------- | ------------------ |
| Input  | Free of charge | $0.50 / 1M tokens  |
| Output | Free of charge | $10.00 / 1M tokens |

#### Free Tier（当前使用）

| 场景                   | 说明                  | 成本      |
| ---------------------- | --------------------- | --------- |
| Preview 模型           | 免费使用              | **$0**    |
| 1000 DAU / 月          | Free Tier 内          | **$0/月** |
| 备用: $25,000 GCP 额度 | 超出 Free Tier 时使用 | -         |

#### Paid Tier（超出 Free Tier 后预估）

> 💡 **Token 计费规则 (官方)**：
>
> - **Input**: $0.50 / 1M text tokens
> - **Output**: $10.00 / 1M **audio tokens**
> - **换算**: **1 秒音频 = 25 tokens**
> - **单价**: 约 **$0.015 / 分钟** ($0.90 / 小时)

| 场景                     | 计算 (预估)              | 成本                  |
| ------------------------ | ------------------------ | --------------------- |
| Output 单价              | $10.00 / 1M audio tokens | -                     |
| 每次单词 TTS (估 1秒)    | 25 tokens × $0.00001     | **$0.00025**          |
| 用户每天 100 词          | 100 × $0.00025           | **$0.025 / 天**       |
| 1000 DAU / 月 (无缓存)   | 1000 × 30 × $0.025       | **$750/月 (≈¥5,400)** |
| 1000 DAU / 月 (90% 缓存) | $750 × 10%               | **$75/月 (≈¥540)**    |

> 📉 **成本分析**：
>
> 按照 **25 tokens/sec** 计算，Gemini TTS 的单词发音成本 (约 $0.00025/词) 虽然略高于 Azure 的纯字符计费 ($0.0001/词)，但差距已从预估的 14 倍缩小至 2.5 倍左右。
>
> 考虑到拥有 **$25,000 GCP 赠金**，这笔开支完全在覆盖范围内。且 90% 的缓存命中率将进一步把月成本降低到忽略不计的水平 ($75/月)。

### 其他服务成本对比

### Google Cloud TTS 成本

#### Standard (标准版)

| 场景                     | 计算                | 成本               |
| ------------------------ | ------------------- | ------------------ |
| 单价                     | $4 / 百万字符       | -                  |
| 每次单词 TTS             | 7 × $0.000004       | $0.000028          |
| 用户每天 100 词          | 100 × $0.000028     | $0.0028            |
| 1000 DAU / 月 (无缓存)   | 1000 × 30 × $0.0028 | **$84/月 (≈¥605)** |
| 1000 DAU / 月 (90% 缓存) | $84 × 10%           | **$8.4/月 (≈¥60)** |

> 💡 免费额度：WaveNet/Neural2 每月前 100 万字符免费，可覆盖约 143,000 次单词播放。

#### WaveNet / Neural2 (高质量版)

| 场景                     | 计算                | 成本                  |
| ------------------------ | ------------------- | --------------------- |
| 单价                     | $16 / 百万字符      | -                     |
| 每次单词 TTS             | 7 × $0.000016       | $0.000112             |
| 用户每天 100 词          | 100 × $0.000112     | $0.0112               |
| 1000 DAU / 月 (无缓存)   | 1000 × 30 × $0.0112 | **$336/月 (≈¥2,419)** |
| 1000 DAU / 月 (90% 缓存) | $336 × 10%          | **$33.6/月 (≈¥242)**  |

> 💡 **缓存策略至关重要**：无论选择哪个服务，90% 缓存命中率可降低 90% 成本。
>
> ✅ **最终选择**：
>
> - **Gemini 2.5 Flash Preview TTS**：Free Tier + $25,000 GCP 额度，成本为 $0，质量优秀，已集成。

---

## 📝 实现步骤清单

### Phase 1: 后端 API

- [x] **1.1** 新增 `/tts/word` 端点（使用 GCP Vertex AI Gemini TTS）
- [x] **1.2** 支持多语言参数（language, voice_name）
- [x] **1.3** 返回 WAV 音频数据（base64）
- [ ] **1.4** 添加 API 测试用例

### Phase 2: 前端基础

- [x] **2.1** 添加 `flutter_tts` 依赖
- [x] **2.2** 创建 `WordTtsService` 服务类
- [x] **2.3** 实现本地 TTS 播放功能
- [x] **2.4** 实现语言支持检测（`getLanguages()`）

### Phase 3: 缓存层

- [x] **3.1** 设计单词音频缓存结构
- [ ] **3.2** 实现内存缓存（LRU）
- [x] **3.3** 实现磁盘缓存（持久化）
- [ ] **3.4** 缓存过期策略

### Phase 4: 混合策略

- [x] **4.1** 实现缓存优先检查
- [x] **4.2** 实现本地 TTS fallback
- [x] **4.3** 实现云端 TTS 请求 + 后台缓存
- [x] **4.4** 错误处理和重试机制

### Phase 5: UI 集成

- [x] **5.1** 在 `VoiceFeedbackSheet` 中添加单词点击事件
- [ ] **5.2** 添加播放状态指示（loading/playing）
- [ ] **5.3** 添加播放动画效果
- [x] **5.4** 处理连续快速点击

### Phase 6: 测试与优化

- [ ] **6.1** 单元测试
- [ ] **6.2** 集成测试
- [ ] **6.3** 性能测试（延迟、缓存命中率）
- [ ] **6.4** 多语言测试

---

## 🔧 技术细节

### 后端 API 设计

```
POST /tts/word
Content-Type: application/json

Request:
{
  "word": "pineapple",
  "language": "en-US",       // 用于自动选择语音
  "voice_name": "Kore"       // 可选，Gemini TTS 语音名称
}

Response:
{
  "audio_base64": "UklGRi...",  // WAV 格式音频
  "format": "wav",               // PCM 24kHz 16-bit mono
  "mime_type": "audio/wav"
}
```

> ✅ **音频格式说明**：
>
> | 服务                     | 格式 | 采样率 | 备注                           |
> | ------------------------ | ---- | ------ | ------------------------------ |
> | **Gemini 2.5 Flash TTS** | WAV  | 24kHz  | PCM 16-bit mono，已包含 header |
>
> 前端播放器需支持 WAV 格式音频。

### Gemini TTS 语音选择

| 语音名称 | 特点                         | 推荐场景   |
| -------- | ---------------------------- | ---------- |
| Kore     | Clear, natural female (默认) | 英语       |
| Aoede    | Warm, expressive female      | 表达性内容 |
| Charon   | Deep, authoritative male     | 正式场合   |
| Fenrir   | Energetic, dynamic male      | 日语       |
| Puck     | Friendly, conversational     | 韩语       |
| Orbit    | Neutral, professional        | 中文兼容   |

### 多语言配置

```dart
class LanguageConfig {
  final String geminiVoice;   // Gemini TTS voice name
  final String localTtsLocale;
  final String displayName;
}

const Map<String, LanguageConfig> languageConfigs = {
  'en-US': LanguageConfig(
    geminiVoice: 'Kore',
    localTtsLocale: 'en-US',
    displayName: 'English (US)',
  ),
  'en-GB': LanguageConfig(
    geminiVoice: 'Kore',
    localTtsLocale: 'en-GB',
    displayName: 'English (UK)',
  ),
  'zh-CN': LanguageConfig(
    geminiVoice: 'Orbit',  // Neutral, 中文兼容
    localTtsLocale: 'zh-CN',
    displayName: '中文（简体）',
  ),
  'ja-JP': LanguageConfig(
    geminiVoice: 'Fenrir',  // Dynamic, 日语适配
    localTtsLocale: 'ja-JP',
    displayName: '日本語',
  ),
  'ko-KR': LanguageConfig(
    geminiVoice: 'Puck',  // Friendly, 韩语适配
    localTtsLocale: 'ko-KR',
    displayName: '한국어',
  ),
  // 扩展更多语言...
};
```

### 缓存结构

```
📁 app_documents/
└── 📁 word_tts_cache/
    └── 📁 {language}/
        ├── {encoded_word}.{format}
        └── ...
```

> 💡 缓存按语言分目录，不按用户分。同一单词的发音对所有用户一致。

#### 文件命名规则

由于不同语言可能包含特殊字符（中文、日语、韩语、撇号、连字符等），文件名需要编码处理：

| 方案              | 示例                                      | 优缺点                           |
| ----------------- | ----------------------------------------- | -------------------------------- |
| URL 编码          | `pineapple.wav`, `%E4%BD%A0%E5%A5%BD.wav` | ⚠️ 大小写问题，长度可能超限      |
| **Hash (MD5)** ✅ | `a1b2c3d4e5f6.wav`                        | ✅ 长度固定，无特殊字符          |
| Base64            | `cGluZWFwcGxl.wav`                        | ⚠️ 可能较长，含 `/` `+` 特殊字符 |

> 📝 **建议**：使用 Hash（MD5）方案，配合元数据文件支持调试。

#### 命名处理流程

```
原始单词 → 统一转小写 → MD5 Hash → 文件名
```

示例：
| 原始单词 | 转小写 | MD5 Hash | 文件名 |
|----------|--------|----------|--------|
| `Hello` | `hello` | `5d41402abc4b2a76...` | `5d41402abc4b2a76.wav` |
| `hello` | `hello` | `5d41402abc4b2a76...` | `5d41402abc4b2a76.wav` |
| `你好` | `你好` | `7eca689f0d3389d9...` | `7eca689f0d3389d9.wav` |

> 💡 **大小写处理**：统一转小写后再 hash，确保 `Hello` 和 `hello` 命中同一缓存。

#### 元数据文件（调试用）

为便于调试，每个语言目录下维护一个 `_metadata.json` 文件：

```json
{
  "5d41402abc4b2a76": {
    "word": "hello",
    "format": "wav",
    "createdAt": "2026-01-16T10:30:00Z",
    "source": "gemini"
  },
  "7eca689f0d3389d9": {
    "word": "你好",
    "format": "wav",
    "createdAt": "2026-01-16T10:31:00Z",
    "source": "gemini"
  }
}
```

缓存目录结构：

```
📁 word_tts_cache/
└── 📁 en-US/
    ├── _metadata.json
    ├── 5d41402abc4b2a76.wav
    └── ...
└── 📁 zh-CN/
    ├── _metadata.json
    ├── 7eca689f0d3389d9.wav
    └── ...
```

#### 缓存查询规则

**查询时应忽略文件扩展名**，以支持格式变更：

```dart
import 'package:crypto/crypto.dart';
import 'dart:convert';

// 生成缓存 key（统一小写 + MD5）
String getCacheKey(String word) {
  final normalized = word.toLowerCase();
  final bytes = utf8.encode(normalized);
  final hash = md5.convert(bytes).toString();
  return hash.substring(0, 16);  // 取前 16 位足够
}

// 查询缓存时，只匹配文件名前缀
Future<String?> findCachedAudio(String word, String language) async {
  final cacheKey = getCacheKey(word);
  final cacheDir = Directory('$basePath/$language');

  if (!await cacheDir.exists()) return null;

  // 查找任意格式的缓存文件
  await for (final file in cacheDir.list()) {
    final fileName = path.basenameWithoutExtension(file.path);
    if (fileName == cacheKey) {
      return file.path;  // 找到缓存，无论格式
    }
  }
  return null;  // 未命中缓存
}
```

这样即使从 MP3 切换到 WAV，现有缓存仍然有效。

### WordTtsService 接口

```dart
abstract class WordTtsService {
  /// 播放单词发音
  /// 自动选择最佳策略（缓存 → 本地 TTS → 云端 TTS）
  Future<void> speakWord(String word, String language);

  /// 预加载单词列表（后台下载）
  Future<void> preloadWords(List<String> words, String language);

  /// 检查语言是否支持本地 TTS
  Future<bool> isLocalTtsAvailable(String language);

  /// 清除缓存
  Future<void> clearCache({String? language});
}
```

---

## 📚 相关资源

### ✅ Gemini 2.5 Flash TTS（当前使用）

- [Vertex AI Gemini TTS 定价](https://cloud.google.com/vertex-ai/generative-ai/pricing)
- [Gemini TTS 文档](https://cloud.google.com/vertex-ai/generative-ai/docs/speech/text-to-speech)
- [后端集成代码](../../backend/src/services/gcp-tts.ts)
- [TTS 端点](../../backend/src/server.ts) - `/tts/gcp/generate` (流式) 和 `/tts/word` (非流式)
- [GCP TTS 实现指南](../../backend/docs/gcp_tts.md)

### 前端

- [flutter_tts 包](https://pub.dev/packages/flutter_tts)
- [发音反馈组件](../lib/features/chat/presentation/widgets/voice_feedback_sheet.dart)
